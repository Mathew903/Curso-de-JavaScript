



/* Nodo

Un nodo en el DOM es cualquier etiqueta del cuerpo, como un parrafo, el mismo body o incluso las etiquetas de una lista. Aunque no siempre son etiquetas. Una etiqueta crea un nodo pero no todos los nodos son etiquetas.

? __Tipos__de__nodos__:

* Document - EL nodo document es el nodo raiz, a partir del cual derivan el resto de nodos.

* Element - Nodos definidos por etiquetas html

* Text - El texto dentro de un nodo element se considera un nuevo nodo hijo de tipo text(texto).

* Attribute - Los atributos de las etiquetas definen nodos, (En JS no los veremos como nodos, sino como informaciòn asociada al nodo de tipo element).

* Comentarios y otros - Los comentarios y otros elementos como las declaraciones doctype en cabecera de los documentos html generan nodos.
*/
