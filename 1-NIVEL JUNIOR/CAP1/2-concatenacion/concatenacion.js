/* Concatenación

-union de cadenas

saludo = 'hola ';
pregunta = ' ¿como estas? ';
respuesta = ' ¿bien y vos?';

frase = saludo + pregunta + respuesta;

document.write(frase);

##############################################

-forzando el string:

resultado = '' + numero1 + numero2; cuando se detecta un string todo automaticamente pasa a ser una cadena de texto, por eso en vez de realizar un operacion, solo se unen los numeros.

-concat: solo funciona para cuando si o si cadena o string.

resultado = numero1.concat(numero2); une a las cadenas

-backtiks(`´) y la variable entre ${}:

resultado = `che ${saludo} que haces`; mejor manera de concatenar.

*/
