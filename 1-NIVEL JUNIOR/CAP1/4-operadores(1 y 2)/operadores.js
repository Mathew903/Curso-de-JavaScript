/* operadores en js

-operadores de Asignación: asigna un valor al operando de la izquierda basado en el valor del operando de la derecha.

let numero = 10;

numero += 5; aca lo que se hace es sumarle 5, seria lo mismo que decir: numero = numero + 5; pero con lo anterior se ahorra codigo o es una forma mas rapida.

document.write(numero);

asignacion: x = y

adiccion: x += y

sustraccion: x -= y

multiplicacion: x *= y

division: x /= y

resto = x %= y

exponencial: x **= y

AND: x &= y

XOR: x ^= y

OR: x |= y

        ##########################################


-operadores Aritméticos: toman valores numericos(ya sean literales o variables) como sus operandos y retornan un valor numerico único.

document.write() este escribe en el documento directamente.

numero1 = 10;

numero2 = 5;

resultado = numero1 + numero2; suma

numero1--; esto es el decremento, solo necesita un valor o una variable, lo que significa acá que al numero se le esta restando 1. En este caso primero se resta y luego se asigna a una varible.

resultado = numero1 / numero2; aca es de division y es necesario 2 valores.

resultado = numero1 ** 2; exponeciacion.

numero1++; incremento funciona como el decremento.

resultado = numero1 % numero2; esto es el resto(remainder).

resultado = numero1 - numero2; resta

unary negation(-), negacion unaria:
resultado = -numero1; nos devuelve un numero negativo

unary plus(+)

resultado = +numero1;

*/
